<!DOCTYPE html>
<html>
  <head>
    <title>MovieApp</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
      <a class="navbar-brand" href="/">映画検索サイト</a>
      <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarTogglerDemo02" aria-controls="navbarTogglerDemo02" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      
      <div class="collapse navbar-collapse" id="navbarTogglerDemo02">
        <ul class="navbar-nav mr-auto mt-2 mt-lg-0">
          <li class="nav-item active">
            <a class="nav-link" href="/">Home <span class="sr-only">(current)</span></a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="#">Link</a>
          </li>
          <li class="nav-item">
            <a class="nav-link disabled" href="#" tabindex="-1" aria-disabled="true">Disabled</a>
          </li>
        </ul>
          <form action="/movie" method="get" class="form-inline my-2 my-lg-0">
            <input class="form-control mr-sm-2" type="search"  name="search" placeholder="Search" >
            <button class="btn btn-outline-success my-2 my-sm-0" type="submit">search</button>
          </form>
      </div>
    </nav>
  <header>
    <div class="header-img">
      <div class="img-text">ここに画像</div>
    </div>
  </header>
  <main>  
    <div class="index_search">
      <div class="search_person">
        <%= form_with(url: movie_index_path,:method => 'get',local: true) do %><%= text_field_tag :search %>
        <%= submit_tag 'Search', :name => nil %>
        <% end %>
      </div>
      <div class="search_movie">
        <%= form_with(url: search_index_path,:method => 'get',local: true) do %><%= text_field_tag :search %>
        <%= submit_tag 'Search', :movie => nil %>
        <% end %>
      </div> 
      <br>
      <ul>
        <li>作品名</li>
        <li>キャスト名</li>
        <li>ジャンル</li>
      </ul>  
    </div>
  </main>  
    <%= yield %>
  </body>
</html>
